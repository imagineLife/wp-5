import initialState from './initialState';
import { boxReducer, boxReducerTypes } from './reducer';
console.log({ boxReducer, boxReducerTypes });

export {
  initialState,
  boxReducer,
  boxReducerTypes
};